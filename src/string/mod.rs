// 这是一个 Rust 模块，用于重排字符串
pub mod reformat_the_string;

// 这是一个 Rust 模块，用于检查一个单词是否是另一个字符串中任意单词的前缀
pub mod check_if_a_word_occurs_as_a_prefix_of_any_word_in_a_sentence;

// 这是一个 Rust 模块，用于字符串旋转
pub mod string_rotation_lcci;

// 这是一个 Rust 模块，用于重新格式化电话号码
pub mod reformat_phone_number;

// 这是一个 Rust 模块，用于检查二进制字符串中是否最多只有一个连续的 1
pub mod check_if_binary_string_has_at_most_one_segment_of_ones;

// 这是一个 Rust 模块，用于找到最短的公共超序列
pub mod shortest_common_supersequence;

// 这是一个 Rust 模块，用于掩盖个人信息
pub mod masking_personal_information;

// 这是一个 Rust 模块，用于找到最长的分块回文分解
pub mod longest_chunked_palindrome_decomposition;

// 这是一个 Rust 模块，用于找到字典序最大的子串
pub mod last_substring_in_lexicographical_order;

// 这是一个 Rust 模块，用于计算发出青蛙叫声需要的最小数量
pub mod minimum_number_of_frogs_croaking;

// 这是一个 Rust 模块，用于计算时钟面板上合法的时间数量
pub mod number_of_valid_clock_times;

// 这是一个 Rust 模块，用于生成一个二进制字符串，其中包含表示 1 到 n 的所有子串
pub mod binary_string_with_substrings_representing_1_to_n;
pub mod lexicographically_smallest_palindrome;
pub mod count_the_repetitions;
